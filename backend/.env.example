# Fichier d'exemple des variables d'environnement
# Copiez ce fichier vers .env et modifiez les valeurs

# Port du serveur Express
PORT=5000

# URL de connexion PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Remplacez 'postgres' et 'postgres' par vos identifiants PostgreSQL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tissea_db"

# Clé secrète pour JWT (IMPORTANT: changez cette valeur en production!)
# Générez une clé sécurisée avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
